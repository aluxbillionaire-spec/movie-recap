<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class MediaSeeder extends Seeder
{
    /**
     * Run the database seeds.
     */
    public function run(): void
    {
        // Media table is typically populated by the application when users upload files
        // This seeder can be used to add sample media for testing purposes
        // For now, we'll leave it empty as media entries are created dynamically
    }
}